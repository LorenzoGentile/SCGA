features <- SCGA:::CreateFeature(bounds=list(rep(0,5),bounds=(rep(5,5))) ,dependence = NULL,condOfExcistance = NULL,
                                 types = c(rep("numeric",3),rep("integer",3)),

                                 )
